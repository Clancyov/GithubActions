name: Python_Script.py

on:
  schedule:
    - cron: '* * * * *'  # Runs every minute

jobs:
  run_script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Run script
        run: python Python_Script.py

      - name: Commit and push changes  # Name of the step
        run: |  # Indicates a multiline shell script is coming
          git config --local user.email "aminachak8182@gmail.com"  # Configures Git with the email for the GitHub Action bot
          git config --local user.name "Clancyov"  # Configures Git with the name for the GitHub Action bot
          git add -A  # Adds all changes to the staging area
          git diff-index --quiet HEAD || (git commit -a -m "Update logs" --allow-empty)  # Checks if there are any changes; if yes, commits the changes with a commit message "Update logs"
          git push origin main  # Pushes the committed changes to the 'main' branch of the repository
